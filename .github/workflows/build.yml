name: Build
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: mkdir output && cp index.html output/
      - uses: actions/upload-artifact@v4
        with:
          name: Clumsy
          path: output/
